{%- assign product_handles = 'nova-plastic-abutment-for-casting||nova-chrome-cobalt-abutment-ucla||nova-ball-attachment||nova-flat-locator-abutment||nova-angled-titanium-pop-clicq-18||nova-angled-titanium-pop-clicq-30||nova-slim-titanium-healing-abutment||nova-surgical-hand-tools' | split: '||' -%}
{%- assign product_titles = 'Plastic Abutment for Casting||Chrome Cobalt Abutment / UCLA||Ball Attachment||Flat Locator Abutment||Angled Titanium Pop-Clicq — 18°||Angled Titanium Pop-Clicq — 30°||Slim Titanium Healing Abutment||Surgical Hand Tools' | split: '||' -%}
{%- assign product_types = 'Restorative||CAD/CAM & Casting||Overdenture||Overdenture||Multi-unit||Multi-unit||Healing||Surgical' | split: '||' -%}
{%- assign product_counts = '2 options||2 options||6 heights||8 heights||4 heights||4 heights||3 heights||2 tools' | split: '||' -%}

<section class="nova-shop color-{{ section.settings.color_scheme }} gradient" style="--nova-section-bg: url('{{ 'nova-bg-products-v1.jpg' | asset_url }}');" aria-labelledby="NovaShopHeading-{{ section.id }}">
  <div class="page-width">
    <header class="nova-shop__heading">
      <div><span class="nova-kicker">Popular components</span><h2 id="NovaShopHeading-{{ section.id }}" class="h1">Shop dental implant components.</h2></div>
      <div><span class="nova-shop__eyebrow">08 selected families</span><p>Explore frequently selected restorative and surgical components, with each configuration organized on its product page.</p><a href="{{ routes.all_products_collection_url }}">View the complete catalog <span aria-hidden="true">→</span></a><span class="nova-shop__status"><i></i> Live inventory · ready to configure</span></div>
    </header>
    <div class="nova-shop__grid">
      {%- for handle in product_handles -%}
        {%- assign card_product = all_products[handle] -%}
        {%- if card_product != blank -%}
        <article class="nova-shop-card">
          <a class="nova-shop-card__media" href="{{ card_product.url }}" aria-label="Shop {{ product_titles[forloop.index0] | escape }}">
            <span class="nova-shop-card__badge">In stock</span>
            <span class="nova-shop-card__index" aria-hidden="true">0{{ forloop.index }}</span>
            {%- if card_product.featured_image != blank -%}
              {{ card_product.featured_image | image_url: width: 900 | image_tag: loading: 'lazy', widths: '320, 480, 640, 900', sizes: '(min-width: 990px) 25vw, 50vw', alt: card_product.featured_image.alt | default: card_product.title }}
            {%- else -%}
              {{ 'product-1' | placeholder_svg_tag: 'placeholder-svg' }}
            {%- endif -%}
          </a>
          <div class="nova-shop-card__body">
            <span class="nova-shop-card__type">{{ product_types[forloop.index0] }}</span>
            <h3><a href="{{ card_product.url }}">{{ product_titles[forloop.index0] }}</a></h3>
            <p>Internal hex · {{ product_counts[forloop.index0] }}</p>
            <div class="nova-shop-card__footer">
              <strong>{% if card_product.price_varies %}From {% endif %}{{ card_product.price | money }}</strong>
              <a class="nova-choose-options" href="{{ card_product.url }}">Choose options <span aria-hidden="true">→</span></a>
            </div>
          </div>
        </article>
        {%- endif -%}
      {%- endfor -%}
    </div>
    <div class="nova-shop__assist">
      <div>
        <span class="nova-kicker">Continue your workflow</span>
        <h3>Every component, clearly organized.</h3>
        <p>Browse the complete product collection or ask NOVA to confirm platform compatibility before ordering.</p>
      </div>
      <div class="nova-shop__assist-benefits" aria-label="Ordering benefits">
        <span><i aria-hidden="true">01</i><strong>50+ products</strong><small>Organized by workflow</small></span>
        <span><i aria-hidden="true">02</i><strong>Live configurations</strong><small>Options shown per product</small></span>
        <span><i aria-hidden="true">03</i><strong>Expert guidance</strong><small>Compatibility support</small></span>
      </div>
      <div class="nova-shop__assist-links">
        <a class="nova-shop__assist-primary" href="{{ routes.all_products_collection_url }}">Browse all products <span aria-hidden="true">→</span></a>
        <a href="/pages/contact">Ask a product specialist <span aria-hidden="true">→</span></a>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "NOVA shop products",
  "tag": "section",
  "class": "section",
  "disabled_on": { "groups": ["header", "footer"] },
  "settings": [{ "type": "color_scheme", "id": "color_scheme", "label": "Color scheme", "default": "scheme-1" }],
  "presets": [{ "name": "NOVA shop products" }]
}
{% endschema %}
